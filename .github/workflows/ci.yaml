name: CI
on:
  push:
    branches: [ main ]
  pull_request: {}

permissions:
  contents: read
  packages: write
  id-token: write

env:
  REGISTRY: ghcr.io
  IMAGE_API: ghcr.io/${{ github.repository }}/api
  IMAGE_WORKER: ghcr.io/${{ github.repository }}/worker

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      # 1) get code
      - name: Checkout
        uses: actions/checkout@v4

      # 2) install Helm (proper way on GitHub)
      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.15.2

      # 3) OPA policy check on rendered Kubernetes manifest
      - name: OPA policy check (Kubernetes)
        run: |
          # render just the API deployment from your chart
          helm template charts/api --show-only templates/deployment.yaml > /tmp/api-deploy.yaml

          # install OPA
          curl -L -o opa https://openpolicyagent.org/downloads/latest/opa_linux_amd64
          chmod +x opa
          sudo mv opa /usr/local/bin/opa

          # run the policy you created in opa/k8s/pod_security.rego
          RESULT=$(opa eval -i /tmp/api-deploy.yaml -d opa/k8s 'data.k8s.pod_security.deny' --format=json)
          echo "$RESULT"

          # fail if the policy returned any "must ..." messages
          echo "$RESULT" | grep "must" && { echo "Policy check failed"; exit 1; } || echo "Policy check passed"

      # 4) docker buildx
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      # 5) login to GHCR
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # ---------- API ----------
      - name: Build API image (load to local docker)
        uses: docker/build-push-action@v6
        with:
          context: ./apps/api
          push: false
          load: true
          tags: |
            api:local
            ${{ env.IMAGE_API }}:${{ github.sha }}

      - name: Trivy scan API (local image)
        uses: aquasecurity/trivy-action@0.20.0
        with:
          image-ref: api:local
          format: 'table'
          severity: 'HIGH,CRITICAL'
          exit-code: '1'

      - name: Push API image to GHCR
        uses: docker/build-push-action@v6
        with:
          context: ./apps/api
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.IMAGE_API }}:${{ github.sha }}
            ${{ env.IMAGE_API }}:main

      # ---------- WORKER ----------
      - name: Build Worker image (load to local docker)
        uses: docker/build-push-action@v6
        with:
          context: ./apps/worker
          push: false
          load: true
          tags: |
            worker:local
            ${{ env.IMAGE_WORKER }}:${{ github.sha }}

      - name: Trivy scan Worker (local image)
        uses: aquasecurity/trivy-action@0.20.0
        with:
          image-ref: worker:local
          format: 'table'
          severity: 'HIGH,CRITICAL'
          exit-code: '1'

      - name: Push Worker image to GHCR
        uses: docker/build-push-action@v6
        with:
          context: ./apps/worker
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.IMAGE_WORKER }}:${{ github.sha }}
            ${{ env.IMAGE_WORKER }}:main
